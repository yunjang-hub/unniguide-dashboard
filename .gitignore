# 데이터 파일 (민감정보 - 절대 업로드 금지)
*.xlsx
*.csv
*.xls

# 생성된 HTML 리포트 (루트 것들만 제외, docs/는 GitHub Pages 배포용이라 포함)
/*.html
/hospitals/

# 시스템
.DS_Store
__pycache__/
*.pyc
.omc/

# Streamlit
.streamlit/cache/
.streamlit/secrets.toml

# 서비스 계정 인증 파일 (절대 커밋 금지)
service_account.json
*service-account*.json
